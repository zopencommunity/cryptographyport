From: Igor Todorovski <itodorov@ca.ibm.com>
Date: 2026-08-12
Subject: [PATCH 1/3] z/OS: Cargo.toml patches for cross-compilation

- Add [patch.crates-io] for pyo3 (z/OS fork), libc (z/OS fork, v0.2.189),
  and target-lexicon (git main for z/OS triple recognition)
- Rename [lib] name from "cryptography_rust" to "_rust" so cargo produces
  lib_rust.so. On z/OS the cdylib's internal GOFF DLL name is set to the
  lib name; naming it "cryptography_rust" causes the loader to look for
  libcryptography_rust.so at runtime (which doesn't exist). With name="_rust"
  the internal DLL name is "lib_rust.so" which is never imported by any other
  .so, so the rename to _rust.cpython-3XY.so in the wheel is invisible to
  the loader.
diff --git a/Cargo.toml b/Cargo.toml
index 79ea7aa3c..8e64c014c 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -36,3 +36,12 @@ self_cell = "1"
 
 [profile.release]
 overflow-checks = true
+
+[patch.crates-io]
+pyo3 = { path = "/tmp/pyo3" }
+pyo3-build-config = { path = "/tmp/pyo3/pyo3-build-config" }
+pyo3-ffi = { path = "/tmp/pyo3/pyo3-ffi" }
+pyo3-macros = { path = "/tmp/pyo3/pyo3-macros" }
+pyo3-macros-backend = { path = "/tmp/pyo3/pyo3-macros-backend" }
+target-lexicon = { git = "https://github.com/bytecodealliance/target-lexicon", branch = "main" }
+libc = { path = "/tmp/libc-zos" }
diff --git a/src/rust/Cargo.toml b/src/rust/Cargo.toml
index 878a23d27..5b4b9c47b 100644
--- a/src/rust/Cargo.toml
+++ b/src/rust/Cargo.toml
@@ -29,7 +29,7 @@ self_cell.workspace = true
 pyo3-build-config.workspace = true
 
 [lib]
-name = "cryptography_rust"
+name = "_rust"
 crate-type = ["cdylib"]
 
 [lints.rust]
